{% extends 'base.html.twig' %}

{% block title %}{{ 'route.name.admin.image_manage' | trans({}, 'routes') }}{% endblock %}

 {% block body %}
     <div class="card shadow">
         <div class="card-body">
             <a href="{{ path('app_admin_image_create') }}"
                class="btn btn-primary shadow mb-5"
             >
                 {{ 'btn.create' | trans }}
             </a>

             {% include 'image/_gallery.html.twig' %}
         </div>
     </div>
 {% endblock %}